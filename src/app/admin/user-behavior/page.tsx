import { UserBehaviorClient } from "@/app/admin/user-behavior/UserBehaviorClient";

export const dynamic = "force-dynamic";

export default function AdminUserBehaviorPage() {
  return <UserBehaviorClient />;
}
